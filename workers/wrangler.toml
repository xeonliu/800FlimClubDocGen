name = "800filmclub-docgen-api"
main = "index.js"
compatibility_date = "2024-09-23"

# 在 Cloudflare Dashboard → Workers → 你的 Worker → Settings → Variables 中配置：
#   TMDB_API_KEY  — TMDB API 密钥
#   OMDB_API_KEY  — OMDb API 密钥
#
# 本地调试：
#   npx wrangler dev
#
# 部署：
#   npx wrangler deploy
